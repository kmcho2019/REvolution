You are performing CodeEvolve exploration on an RTL design task.

CONTEXT_JSON:
{context_json}

Instructions:
- Produce a materially different approach from the current parent or seed.
- Use inspirations as semantic hints, not text to copy blindly.
- Favor diversity in architecture, control structure, or decomposition when reasonable.
- Maintain synthesizable RTL and respect the benchmark task description.
- Keep the module interface stable and avoid introducing multiple drivers or inconsistent reset behavior.
- If the parent failed, prioritize a compile-clean, functionally correct repair before larger structural changes.

Return the required JSON object only.
