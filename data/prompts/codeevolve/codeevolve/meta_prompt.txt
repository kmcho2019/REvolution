You are evolving a CodeEvolve prompt for RTL search.

CONTEXT_JSON:
{context_json}

Rewrite the current prompt so it better guides future RTL generation for this task.
Focus on clearer priorities, better failure avoidance, and stronger use of useful solution context.

Return the required JSON object only, where `code` contains the full evolved prompt text.
