Evolve the parent RTL toward the design intent in thought_spec.
The problem description is authoritative when it conflicts with the thought.
Preserve and refine the parent's working low-level structure where it is
compatible with the thought (this is how PPA improves incrementally);
rewrite only the parts the thought's architecture genuinely requires.
Re-derive interface names, bit indexing, and value tables from the problem
description itself, never from the parent code.

CONTEXT_JSON:
{context_json}

Return exactly ONE JSON object and nothing else:
{
  "format": "eoh_v1",
  "mode": "whole",
  "thought": "<brief note on what was preserved vs changed>",
  "code": "<full, runnable Verilog as one JSON string>"
}
