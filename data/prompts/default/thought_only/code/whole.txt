Implement one RTL realization from the thought_spec.
The problem description is authoritative if it conflicts with the thought.
Do not read or refer to parent code, parent feedback, code-level logs, failed
sibling samples, or repair transcripts.

CONTEXT_JSON:
{context_json}

Return exactly ONE JSON object and nothing else:
{
  "format": "eoh_v1",
  "mode": "whole",
  "thought": "<brief implementation rationale for this sample>",
  "code": "<full, runnable Verilog as one JSON string>"
}
