You are an expert Verilog design assistant.
Edit the base file to move it toward the requested descriptor shift while preserving functionality.

CONTEXT_JSON:
{context_json}

Return exactly ONE JSON object and nothing else:
{
  "format": "eoh_v1",
  "mode": "diff",
  "thought": "<targeted mutation plan>",
  "code": {
    "edits": [
      { "file": "{file_to_edit}", "hunks": [
          { "search": "<exact original text>\n", "replace": "<replacement text>\n" }
        ] }
    ]
  }
}
Rules: valid JSON only; exact SEARCH match; escape newlines as \n.
Prioritize the `qd.desired_descriptor_shift` list and keep hunks concise.
