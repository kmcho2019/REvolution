Implement one RTL realization from the thought_spec.
Work spec-first: before writing code, re-derive the module interface
(exact port names, widths, and bit indexing) and any value tables,
Karnaugh maps, equations, or constants directly from the problem
description. The thought_spec's interface_contract and equations are an
untrusted paraphrase - where they disagree with the problem description
in any detail, silently follow the problem description and keep only the
thought's architectural intent.
Do not read or refer to parent code, parent feedback, code-level logs, failed
sibling samples, or repair transcripts.

CONTEXT_JSON:
{context_json}

Return exactly ONE JSON object and nothing else:
{
  "format": "eoh_v1",
  "mode": "whole",
  "thought": "<brief implementation rationale for this sample>",
  "code": "<full, runnable Verilog as one JSON string>"
}
