Audit complete. All reads were static; no files were edited, no benchmarks or background processes launched, and I did not re-run pytest (test-failure claims below are verified statically).

# Verdict: FAIL

The FAIL is driven by unmet frozen prelaunch requirements, not by method defects: every implementation-integrity check passed (classic core byte-identical, provenance accurate, treatment confined to the two registered selection blocks, no QD state, deterministic ties, disclosed scalar residual). Under this goal's own contract language, the items below must be resolved before the seed-42 smoke or any full-RTLLM evidence.

## Blocking findings

1. **Four failing tests at the frozen implementation commit violate Pre-Launch Freeze item 8.** `pareto_revolution_validation_plan.md:268-269` requires "Unit, integration, classic-regression, operator-contract, and artifact tests pass at the frozen implementation commit." Statically confirmed: `tests/revolution/test_candidate_evaluator_parity.py:60` builds the legacy engine as a bare `SimpleNamespace`, and unchanged `src/revolution/algorithm.py:1295` calls `self._extract_candidate_descriptor_values` — an `AttributeError` in every case that reaches synthesis (3 parametrized cases at `test_candidate_evaluator_parity.py:107-185` plus the test at line 212), exactly matching the documented "3 passed, 4 failed" (`pareto_revolution_validation_implementation_history.md:267-274`). This is pre-existing debt, honestly recorded — but the failing suite is the **strict-ablation parity check**, the exact evaluation mode this experiment mandates, and it currently blinds parity verification on the success path whose `status=="success" ⇒ ppa complete` invariant the treatment's assertions rest on (`selection.py:46-49`). The repair is test-fixture-only; `algorithm.py` must remain untouched (frozen SHA-256).

2. **Frozen-contract test suite is incomplete, including one checked-but-only-half-verified item.** (a) Boundary-front crowding truncation, stable-tie, and population-cap tests — open at `pareto_revolution_validation_implementation_todo.md:58-59`. (b) Classic seeded selection-regression test — open at todo line 61; the plan's correctness gate (`pareto_revolution_validation_plan.md:329-330`) requires it in addition to the hash test that does exist (`test_pareto_revolution.py:284-286`). (c) Todo line 60 is checked ("changing scalar score alone cannot change Pareto selection") but `test_pareto_revolution.py:156-176` covers **parent** selection only, while the frozen gate (`plan:323-324`) covers parent **and survivor** decisions — survivor-side scalar invariance is untested. (d) Post-hoc front reproduction test and the delivered-front reporting-only proof — open at todo lines 49 and 63; the plan's correctness gate (`plan:334-335`) requires reproducibility from generation logs.

3. **V2's smoke sentence "Runtime must assert those objective counts" is not implemented.** `pareto_revolution_claims_addendum_v2.md:64-70` requires the seed-42 runtime to assert 2/3/3 axes for the three problems. Currently the axis count is *derived* from data (`src/revolution/runtime/problem_spec.py:87-108,129-134` infers circuit type from the `_ppa.txt` proxy timing) and *recorded* in `backend_details` (`src/revolution/backends/revolution_backend.py:422-439`) but never asserted against the frozen expectation. I verified the data currently resolves correctly (Prob003 `eff_clk_period 0.0` → combinational/2; Prob025 `0.19` → sequential/3; Prob006 has no `_ppa.txt` → frozen sequential/3 raw), so this is an assertion gap, not a live wrong-axis bug — but the frozen contract demands assert, not record.

4. **The Stage-0 command freeze is still open.** Todo lines 36-37 ("Freeze the one Pareto arm, commands, output roots, report commands, and stop/promotion rules before any evidence run") is unchecked, and the plan's Pre-Launch Freeze chapeau (`plan:246-247`) forbids any LLM-backed evidence run — the smoke included — until all items are recorded. The matched fresh-classic smoke arm's exact command belongs here too.

## Optional findings

- `ParetoEoHEngine.__init__`'s contract asserts (`engine.py:37-46`) are never executed by any test: the unit test bypasses them via `object.__new__` (`test_pareto_revolution.py:247`) and the backend test injects a fake engine (`test_revolution_backend.py`, `_FakeParetoEngine`). A trivial construction test would close this.
- The engine does not assert `evaluation_mode == "strict_ablation"`; only the CLI does (`scripts/run_backend.py:1436-1449`). Tolerable because `candidate_objectives` (`selection.py:46-49`) fails fast if a non-strict success ever appears, but the engine's stated posture of "repeating method-critical assertions" stops one short here.
- CLI contract rejection is tested for `classic_operator_kind` only (`tests/scripts/test_run_backend.py:351-370`); the other three rejected knobs (operator set, evaluation mode, representation kind) have no negative test.
- Cosmetic loop divergences, harmless but relevant for log-diff audits: the classic "No valid … strategies available. Skipping..." prints (`algorithm.py:3831,3884`) have no Pareto counterpart (`engine.py:96-97,136-137`), and the completion print labels differ (`engine.py:250` vs `algorithm.py:4144`).
- `test_environmental_selection_uses_successes_then_new_failures` gives failures finite scores (`test_pareto_revolution.py:209-210`), a state unreachable in the real pipeline where failures are pinned to `-inf` (`algorithm.py:1165,1200,1222,1322`, corroborated by `test_candidate_evaluator_parity.py:208-209`). Fine as an ordering check; noting so nobody reads it as a reachable regime.
- The V2 config label `evidence_role: technical_and_directional_only` (`pareto_revolution_rtllm_ablation_v2.yaml:29`) is superseded by V3 clause 1 (technical only); the smoke package must use the V3 language.

## Treatment-drift assessment

**No unregistered drift found.** The committed treatment matches the registered mechanism exactly:

- Parents: one ranking per generation (`engine.py:73-77`), uniform without-replacement two-contestant tournaments via `random.sample` on the run-seeded global RNG (`selection.py:114`; seeding at `scripts/run_backend.py:642-645`) per V3 clause 2; singleton wins directly (`selection.py:114`); key is rank → crowding → unique insertion index (`selection.py:102-107`); C-F's second tournament excludes the first winner by candidate id (`selection.py:127-131`); C-F disabled below two successes exactly as classic (`engine.py:127-129` = `algorithm.py:3871-3873`).
- Survivors: candidate set is exactly previous Success pool + all new offspring (`engine.py:223-229`, `selection.py:145` — identical set to classic's `algorithm.py:4049`); total-order sort by (rank, −crowding, insertion) with front-local crowding (`archive.py:130-154,435-457`) is equivalent to fill-complete-fronts-then-crowding-truncate; failure fill preserves classic's shuffle-then-score-descending order (`selection.py:159-164` ≙ `algorithm.py:4051-4052`); previous fails dropped, matching classic.
- Objectives: normalized reference gains vs negative raw active PPA branch on `supports_reference_ppa` (`selection.py:63-72`); the frozen four-task unknown mapping is exact and every other unknown fails (`selection.py:16-36`), matching `prelaunch_audits.md` and the V2 config; the classic synthetic reference never touches selection.
- Scalar residual disclosed and confined: score drives only UCB reward (`engine.py:196-221`) and feedback, never `_selection_key`; `quality_score` is stored in `ArchiveMember` but unused by ranking.
- No descriptors/cells/archive/capacity/warmup/emitter/trigger anywhere in `src/revolution/pareto_revolution/` (`descriptors=()` at `selection.py:87` is the only descriptor token); no try/except, fallbacks, or optional knobs; the mode is one discriminated value with exhaustive dispatch (`revolution_backend.py:160-168`) and CLI contract rejection. Operator surface is type-frozen: `CLASSIC_SUCCESS_STRATEGIES` (`algorithm.py:95-101`) excludes M-T/C-D, and the asserted `eoh_success_operator_set == "classic"` + `classic_operator_kind == "eoh_strategies"` (`engine.py:40-41`) make single-thought unreachable. The dropped `parent.status == "success"` reward precondition (`engine.py:196-205` vs `algorithm.py:3997-4017`) is provably dead code given success-pool membership plus the selection asserts. Code style follows the repo's simplicity rules; ~420 source lines total, a natural controlled extension for the stated TCAD ablation.

## Copied-loop provenance assessment

**Accurate.** `git diff 9702534157..HEAD -- src/revolution/algorithm.py` is empty; the worktree file hashes to the frozen `78ebc901…655`, matching `prelaunch_audits.md:76-77` and `test_pareto_revolution.py:23-25`. I compared both loops line by line: all five recorded mappings in `pareto_revolution_validation_implementation_history.md:244-261` check out, and the only behavioral deltas are the two registered selection blocks (classic `3888-3921` → `engine.py:139-142`; classic `4048-4129` → `engine.py:223-236`). Removed branches (single-pool allocation/redivision, single-thought, non-classic operator paths) are all unreachable under the asserted invariants; the probabilities-dict construction moved after the identical STOP check (`engine.py:167-172` vs `algorithm.py:3953-3959`) with no behavioral effect. All other recorded hashes (V2 config, narrative, addenda V2/V3) also match. Commit `3f5f3c999d` is documentation-only.

## Test assessment

The 11 focused Pareto tests (`tests/revolution/test_pareto_revolution.py`) cover the frozen-mapping exactness, unknown-type rejection, all three objective paths (2-axis normalized, 3-axis normalized, 3-axis negative-raw), the success-invariant violation, parent-side scalar invariance, distinct C-F parents at both selection and engine level, environmental selection with failure fill, and the classic hash freeze. Backend dispatch (`test_revolution_backend.py:141-162`) and CLI accept/reject (`test_run_backend.py:221-227,351-370`) are wired. The gaps are Blocking findings 1–2 and the first three Optional findings; the documented four broader-suite failures are confirmed pre-existing, outside both Pareto commits, and mechanically explained by the stale parity fixture against unchanged classic code.
