# Claude Read-Only Scaffold Re-Review

Date: 2026-07-11 UTC.
Command: `claude -p`, focused read-only prompt, 900-second timeout.
Result: completed with exit code 0.
Verdict: `FAIL`.

The output below is preserved verbatim.

FAIL

# Second Adversarial Review: `pareto_revolution_validation` revised scaffold

**Scope of verification.** I read the prior FAIL review and all seven current scaffold files, diffed every revision against the committed scaffold (`de6e0851b6`), and re-verified the contract against the code and manifests it binds: all four locked SHA-256 hashes reproduce; all nine legacy-holdout RTLLM tasks are confirmed inside the 46-task manifest (the disclosure at `pareto_revolution_validation_plan.md:87-91` is accurate); no `revolution_pareto`/`pareto_revolution` code pre-exists; both status-assignment paths (`src/revolution/algorithm.py:1298-1306`, `4357-4366`) set `status == "success"` only when synthesis, functionality, and `ppa_success` all hold, so the plan's Success/PPA invariant (`plan.md:141-145`) matches the code; the "existing seeded-shuffle then score-descending" fill order cited at `plan.md:133-135` genuinely exists (`algorithm.py:4050-4052`, `4109-4116`), previous fails genuinely are not retained (`algorithm.py:4049`, `4119-4125`), C-F disable-below-two and distinct-parent behavior match classic (`algorithm.py:3872-3873`, `3909-3911`), and the new disclosure that the prior NSGA-II positive used a truncated pool with a champion lane, not this tournament (`plan.md:68-71`), is accurate against `src/revolution/qd_natural/engine.py:28-74`. Fresh-holdout feasibility has ample headroom: 156 VerilogEval-Spec-to-RTL tasks exist in `data/bench/`, at most 48 unique problems appear across all development manifests combined.

**All three original blockers are substantively resolved, and the six remaining findings were dispositioned faithfully.** Blocker 1: the promotion surface (full RTLLM, 46-task gates, `plan.md:335-349`) and the paper-facing gate (fresh VerilogEval-only holdout, `plan.md:353-365`) are now benchmark-disjoint by construction; the legacy manifest is demoted everywhere consistently (`plan.md:203`, `224-225`; `goal_template.md:42-44`; todo:30-31); rewritten precondition 5 (`pareto_revolution_validation_adversarial_prompt.md:34-36`) is satisfiable on an honest outcome; no "untouched" language survives. Blocker 2: no-reference tasks are pinned to negative raw active PPA with a mathematically correct affine-invariance rationale (`plan.md:113-118`), Success-without-PPA is asserted away as an invalid run (`plan.md:141-145`), and the survivor rule is stated completely with the classic-divergence consequence disclosed (`plan.md:128-136`). Blocker 3: precondition 2 (`adversarial_prompt.md:27-29`) now fails on *any* unregistered treatment and on a second registered Pareto treatment, while explicitly excluding registered comparator and technical-smoke configurations — so the stage-2 smoke and the matched classic comparator are not accidentally forbidden. Optionals 4–7 are all folded in (`plan.md:68-71`, todo:17-18, `plan.md:327-329`, `plan.md:346`/`adversarial_prompt.md:86`).

The FAIL rests on one defect in the remedy itself.

## Findings (ordered by severity)

**1. BLOCKING — The fresh-holdout selection rule is not deterministic, contradicting the scaffold's own "fixed rule" and "deterministically freeze" claims.**
`plan.md:240-246` pins size (20), benchmark, seed `20260711`, "stratified by circuit type," no functionality filter, and the exclusion snapshot — but a seed without a pinned procedure pins nothing. Unspecified: (a) the stratum allocation (proportional to inventory? equal split? what rounding?); (b) the sampling procedure the seed drives (RNG construction, per-bucket vs global draw, ordering); (c) the source-inventory artifact defining "reference-PPA-complete" for VerilogEval — no such manifest exists in the repo (the RTLLM equivalent came from the 20260630 sweep tables; `baselines/hard_iteration_subset_vanilla_openai_gpt_oss_120b.csv` has circuit types and gate counts but no reference PPA), so completeness must be established by a reference-synthesis sweep the rule never names or hashes. "Archive the source inventory, exclusion audit, selection command, and hashes" makes the freeze reproducible *post hoc*, but preregistration is the point: two faithful implementers running item 5 today would freeze different manifests for the goal's single paper-facing gate set. This falls to exactly the standard the prior review's blocker 2 applied, and it contradicts `goal_template.md:45` ("freeze a new 20-task, prior-run-disjoint VerilogEval reference manifest **with the plan's fixed rule**") and the disposition's "deterministically freeze" (`pareto_revolution_validation_implementation_history.md:84`). The repo's own precedent shows the required standard: the legacy holdout pinned source CSV + SHA-256, stratified-proportional allocation with largest-remainder rounding, and the exact per-bucket RNG string (`data/configs/holdout_reference_subset.yaml:6-13`).
*Failure scenario:* the goal activates; at freeze time the implementer chooses an inventory definition and stratum split with no registered rule; the adversarial validator can verify disjointness (`adversarial_prompt.md:76-77`) but cannot verify rule-conformance of a rule that does not determine an outcome; a paper reviewer asks how the holdout was selected and the honest answer is "by implementer discretion at freeze time, seeded" — degrading the very credibility the fresh-holdout remedy was adopted to buy.
*Minimal fix (two sentences in `plan.md` item 5):* "Inventory: all VerilogEval-Spec-to-RTL tasks whose reference design yields complete active PPA under the frozen toolchain, established by one archived, hashed reference-synthesis sweep, with circuit-type labels from that sweep. Selection reuses the `holdout_reference_subset_v1` rule: stratified proportional over circuit type with largest-remainder rounding, per-bucket seeded shuffle `random.Random(f'20260711:VerilogEval-Spec-to-RTL:{circuit_type}')`, taking each bucket's quota in shuffled order." No other file needs edits (`goal_template.md:45` already delegates to "the plan's fixed rule").

**2. OPTIONAL — Precondition 3 can be read to auto-FAIL the registered V2 descriptive comparator.** "Any evidence run used … descriptors, cells, QD retention" (`adversarial_prompt.md:30-31`) predates this revision, but precondition 2 now carves out registered comparators (`adversarial_prompt.md:29`) while precondition 3 does not, and the registered Smooth-QD V2 comparator roots (`plan.md:217-219`) are runs that used QD retention and would appear in the evidence package. Scope it: "Any treatment or fresh comparator run launched for this goal used …".

**3. OPTIONAL — The crowding-truncation tie rule is stated only in the parent step.** The tie sentence "stable insertion order and candidate id" sits in step 3 (`plan.md:124-125`); step 4's boundary-front truncation (`plan.md:128-130`) can tie (multiple infinite-distance extremes, duplicate objective vectors), and the reused `ranked_front` returns ranks/distances without a truncation order (`src/revolution/qd/archive.py:113-151`). The existing engine idiom (`qd_natural/engine.py:60-67`) makes convergent implementation likely, but pin one sentence — "the step-3 tie rule applies to crowding-distance truncation" — in the plan or the mandatory method addendum.

**4. OPTIONAL — Freeze-snapshot vs precondition-5 temporal window.** Exclusion is computed "as of starting commit `de6e0851b6`" (`plan.md:244-245`) but precondition 5 fails on appearance in any run "before its freeze" (`adversarial_prompt.md:34-35`). A task entering a run root between the starting commit and the freeze would satisfy the rule yet trip the precondition. Add to item 5: the exclusion audit re-verifies against run roots present at the freeze commit; a violation bumps the manifest version with recorded rationale.

**5. OPTIONAL — The revision is uncommitted.** All six revised scaffold files plus `reviews/` are working-tree-only (git status). Before activation, commit the revision and this review as `docs(goal)` commits so the freeze rule, rubric, and review trail are themselves in history.

## Readiness

**Not ready to activate as-is.** Everything the prior FAIL blocked on is genuinely fixed, mutually consistent, and consistent with preserving revision 3 unchanged as the historical contract plus a versioned candidate addendum; the method contract now yields one implementation (modulo finding 3's one-sentence pin); the goal remains narrow, natural, and evidence-derived. The single remaining blocker is a documentation-level edit of roughly two sentences in `plan.md` freeze item 5. After that fix, the scaffold is ready to activate as a research goal — which, per the rubric, asserts registration integrity only, not experimental evidence for the method.
