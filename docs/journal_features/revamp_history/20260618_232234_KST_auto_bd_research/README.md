# Auto-BD Research Scaffold

Start with `START_HERE.md`.

The controlling contract is `auto_bd_research_plan.md`. When documents
conflict, that file wins.

The current locked inputs are `auto_bd_subset_lock.yaml`,
`auto_bd_task_catalog.md`, and `auto_bd_run_policy_lock.yaml`.
