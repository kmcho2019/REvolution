# Claude Read-Only Scaffold Review

Date: 2026-07-11 UTC.
Command: `claude -p`, read-only prompt, 900-second timeout.
Result: completed with exit code 0.
Verdict: `FAIL`.

The output below is preserved verbatim.

FAIL

# Adversarial Review: `pareto_revolution_validation` draft goal scaffold

**Scope of verification.** I read all seven scaffold files, the six grounding sources, the advisor bundle (shortlist, executive summary, validation plan, advisor questions, independent review), and the actual engine code the contract binds: classic selection in `src/revolution/algorithm.py`, the Pareto primitives in `src/revolution/qd/archive.py` and `src/revolution/qd/scoring.py`, the V2 mechanism in `src/revolution/qd_natural/engine.py`, and the locked manifests. All four SHA-256 hashes in the plan reproduce exactly. The named reuse primitives exist (`active_ppa_objectives` at `src/revolution/qd/archive.py:51`, `ranked_front` at `archive.py:113`, `compute_ppa_gains` at `src/revolution/qd/scoring.py:48`), unknown circuit types already fail explicitly (`archive.py:58`), no `revolution_pareto` mode pre-exists, the TODO is 113/120 lines, the evidence numbers in the plan match doc 13, and the scaffold is committed as one `docs(goal)` commit (`de6e0851b6`). The overall design — one frozen method, three exclusive completion paths, HV-AUC subordinated to final HV, reference-seeded optimization explicitly deferred — is sound. The FAIL rests on three specific defects, all fixable with documentation-level edits.

## Findings (ordered by severity)

**1. BLOCKING — The held-out gate is contaminated by the promotion surface by construction, making the rubric's precondition 5 unsatisfiable and the "untouched" language false.**
The held-out manifest (`data/configs/holdout_reference_subset.yaml:38-48`) contains 9 RTLLM problems (Prob009, Prob012, Prob014, Prob023, Prob026, Prob027, Prob039, Prob042, Prob043). I verified all 9 are inside the locked 46-task gate manifest (`docs/journal_features/revamp_history/20260622_010615_KST_useful_bd_push/RTLLM_full_suite/20260630/tables/rtllm_reference_complete_manifest.yaml:9,12,13,21,24,25,37,39,40`). The plan makes full RTLLM the stage 3–5 promotion surface with all gates "on the locked 46-problem manifest" (`pareto_revolution_validation_plan.md:294-296`) at the same seeds 1001–1005 later reused for held-out runs (`plan.md:237`, todo line 81). So 45% of the "untouched 20-problem held-out set" (`plan.md:314`, `goal_template.md:40`, todo line 80) directly influences stage promotion — and already influenced candidate selection, since the prior classic/V2/S07 five-seed campaigns ran these problems repeatedly. The adversarial prompt's hard precondition 5 ("Held-out problems influenced … stage promotion before the final held-out launch," `pareto_revolution_validation_adversarial_prompt.md:31-32`) then forces FAIL on every outcome, including an honest one.
*Failure scenario:* the goal executes perfectly, reaches stage 6, and the final validator must either FAIL a correct result or quietly ignore precondition 5 — inviting exactly the post-hoc rationalization the scaffold exists to prevent. Alternatively, a "primary positive" is published on a held-out set that a reviewer can show was 45% inside the screening suite at the same seeds.
*Minimal fix:* in the plan (and mirrored in `goal_template.md` and the adversarial prompt), (a) disclose the 9-problem overlap explicitly; (b) predeclare that stage 3–5 promotion gates are computed on the 37 non-held-out reference-complete tasks (46-task values reported descriptively for comparability with prior campaign numbers — feasible since the canonical reporter must emit per-unit rows); (c) replace "untouched" with accurate language and add a predeclared held-out sensitivity row (VerilogEval-only stratum, or leave-overlap-out) to show the REF_WIN verdict is not carried by the overlapping RTLLM problems; (d) reword precondition 5 accordingly.

**2. BLOCKING — The "Frozen Method Contract" underspecifies three edges where independent implementers will build different algorithms.**
(a) *Reference-incomplete tasks.* Step 1 of the changed mechanism ("Convert each successful current candidate to maximize-form normalized PPA gains against the reference," `plan.md:102-103`) is undefined on the 4 of 50 RTLLM tasks with no reference PPA, yet the plan mandates running all 50 (`plan.md:295-296`). Classic silently substitutes synthetic "default high PPA values" (`src/revolution/algorithm.py:815-826`); an implementer could inherit that fallback, exclude the tasks from Pareto ranking, or treat it as an error — three different search behaviors, one of which arguably violates "no adaptive fallback to scalar selection" (`plan.md:141`).
(b) *Success candidates without valid PPA.* Classic's survivor logic distinguishes `status == "success"` from `ppa_success` (`algorithm.py:4060-4062`); such candidates cannot be ranked on gain objectives. The contract never says whether they enter the tournament/environmental selection, fall to the failure fallback, or are asserted away.
(c) *The "existing classic failure fallback" is mischaracterized.* Classic does not fill fail slots only after successes: it score-sorts a mixed pool where a 0-score failure can displace a negative-score success, plus guarantees per-metric champions (`algorithm.py:4048-4116`). "Preserve the existing classic failure fallback when successful candidates do not fill the population" (`plan.md:112-113`) therefore describes behavior classic does not have and leaves the success/fail slot policy ambiguous in the negative-gain regime.
*Failure scenario:* two faithful implementations of `plan.md` §Frozen Method Contract diverge on the 4 no-reference tasks and on negative-gain generations, producing different HV/coverage numbers under the "same frozen method" — indistinguishable, after the fact, from an unregistered variant.
*Minimal fix:* add one short paragraph to the Frozen Method Contract (and require the same in the claims addendum) pinning: no-reference tasks reuse the classic default-reference fallback with the note that per-axis normalization is monotone so dominance equals raw-metric dominance (or exclude those 4 tasks from Pareto objectives with a stated rule); success-without-valid-PPA candidates are excluded from Pareto ranking and handled with failures; and the survivor rule stated completely — NSGA-II environmental selection over {previous Success pool ∪ successful offspring with valid gains}, remaining slots filled by the stated fallback ordering — with the acknowledged consequence that fail-pool dynamics differ from classic's mixed score sort.

**3. BLOCKING — Adversarial hard precondition 2 tolerates one unregistered method configuration.**
"More than one unregistered method configuration entered the decision" (`pareto_revolution_validation_adversarial_prompt.md:27`) fails only at two or more unregistered configurations; exactly one unregistered variant passes the binding enforcement instrument, contradicting the TODO completion gate ("No unregistered variant … entered the decision," todo line 104).
*Failure scenario:* an implementer runs one unregistered tweak, folds it into the decision, and the validator following the rubric literally cannot FAIL on precondition 2.
*Minimal fix:* reword to "Any unregistered method configuration entered the decision, or more than one method configuration was run as evidence."

**4. OPTIONAL — The specified parent selection is not the mechanism the evidence demonstrated.**
The V2−V1 NSGA-II benefit cited as the hypothesis basis (`plan.md:64-66`) was produced by a rank/crowding-truncated global pool with uniform draws plus a champion lane (`src/revolution/qd_natural/engine.py:28-74`, mirroring `QDEngine._nsga2_global_pool`), not by the binary tournament specified at `plan.md:107-109`. Binary tournament is the defensible "standard NSGA-II" choice and is parameterless, but the claims addendum should disclose the difference so "retaining the demonstrated NSGA-II benefit" is not overstated, and should pin tournament minutiae (contestant sampling with/without replacement; the exact distinct-winner procedure for C-F).

**5. OPTIONAL — Parent+survivor bundling leaves component attribution open; make the advisor decision explicit in the approval gate.**
The history correctly flags this ("Draft Decision Needing Review," `pareto_revolution_validation_implementation_history.md:47-52`) and the advisor bundle asks it directly (advisor_questions.md, Method Q1), but the TODO's approval checkbox (todo lines 17-18) does not name it. Add "including the parent+survivor bundling decision" to that item so activation cannot proceed with the question silently unresolved.

**6. OPTIONAL — Method-inherent budget skew has no closure class.**
The budget gate says a >±10% token/call skew "blocks promotion until an infrastructure cause is corrected" (`plan.md:286-289`). If the skew is method-inherent (Pareto-selected parents systematically producing longer prompts), no infrastructure fix exists and the goal has no defined exit. State that a persistent method-inherent skew closes as budget-asymmetric supporting/negative evidence, per revision 3's demotion rule (`journal_narrative.md:167-169`).

**7. OPTIONAL — Misleading gate name.**
"Five-seed held-out promotion" (`plan.md:304`) is actually the five-seed *full-RTLLM* gate that unlocks the held-out stage. Rename to "five-seed full-RTLLM promotion" to prevent gate confusion during mechanical application.

## What the review confirms is sound

Against the eight review questions: the candidate is a genuine, natural, falsifiable one-factor extension grounded in the advisor bundle's ranking (Q1, modulo findings 4–5); the coverage-definition split and the S07/S32 same-root discrepancy gate are well designed and grounded — I confirmed the discrepancy in doc 13's own tables (classic seed-1001 root at HV 0.111401 reports 33/46 in the S23/S11 packages and 24/46 in the S32 package) (Q3, modulo finding 1); the three completion paths plus the "missed performance gate is not blocked" rule effectively prevent open-ended tuning (Q4, modulo finding 3); the implementation boundary (isolated `pareto_revolution/` package, minimal hook extraction with a seeded classic regression test) is the right shape given that both selection points are currently inline in `evolve_one_generation` (Q5); the scalar-residual disclosure is honest and matches the code — scalar score genuinely remains in UCB rewards (`algorithm.py:4004-4017`) and prompt feedback, and the "Pareto-aware population selection" claim plus the score-invariance correctness gate handle it correctly (Q6); comparator, operator, budget, seed, reporting, and adversarial contracts are otherwise complete and consistent with revision 3 (Q7); and reference-seeded optimization is cleanly excluded (Q8).

## Readiness

After the three blocking fixes — all edits within the scaffold's own documents plus one predeclared gate-denominator decision, requiring no code and no re-scoping — the scaffold is ready to activate as a research goal. Findings 4–7 can be folded into the mandatory pre-launch claims addendum rather than blocking activation.
