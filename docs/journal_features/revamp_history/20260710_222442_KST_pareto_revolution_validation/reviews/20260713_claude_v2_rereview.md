# Claude V2 Prelaunch Re-Review

Date: 2026-07-13 UTC.
Command: `claude -p`, read-only prelaunch prompt, 900-second timeout.
Result: completed with exit code 0 after approximately twelve minutes.
Verdict: `PASS`.

The output below is preserved verbatim.

# Verdict: PASS

The V2 contract is safe to implement and run. All seven V1 blockers are genuinely resolved and verified against on-disk data, code, and git state — not just against the documents' own claims. I found no blocking contradictions, no unfair comparator behavior, no claim inflation, and no unnecessary code-boundary expansion. Six optional findings follow. (Per the contract's own framing: this PASS certifies the execution contract, not experimental validation.)

## Verification of the V1 blockers (all resolved, with evidence)

1. **Frozen circuit types and active objective counts.** The V2 config freezes exactly `Prob006/013/018/040` as sequential (`pareto_revolution_rtllm_ablation_v2.yaml:95-99`). I verified on disk that these are exactly the four of 50 RTLLM tasks with no `_ppa.txt` (50 prompts, 46 PPA files; all four `ls` misses match), which is precisely what makes `circuit_type == "unknown"` (`src/revolution/runtime/problem_spec.py:87-108`). All four reference RTL files contain `posedge`-clocked always blocks (Prob040 in both clock domains, matching the audit table in `prelaunch_audits.md:29-34`). `active_ppa_objectives` already raises on `"unknown"` (`src/revolution/qd/archive.py:58`), so reject-other-unknowns is the natural code path. Critically, classic's own fitness treats these four as 3-axis sequential (synthetic `T_ref = clk_period ≠ 0` → `is_sequential=True`, `src/revolution/algorithm.py:820-826, 936-945`), so the frozen sequential mapping matches classic's effective axis count — a fair matched choice, not a treatment advantage.
2. **Smoke exercises all three objective paths.** `Prob003_adder_32bit_ppa.txt` has `eff_clk_period 0.0` → combinational, 2-axis normalized; `Prob025_sequence_detector_ppa.txt` has `0.19` → sequential, 3-axis normalized; Prob006 has no reference → 3-axis raw. Config pins all three with `objective_count` assertions (`v2.yaml:27-39`). I also confirmed the V1 blocker was real: `Prob024_fsm_ppa.txt` has period `0.0`.
3. **Stage/outcome consistency after holdout infeasibility.** Plan Stage 6 is the unavailable row (`pareto_revolution_validation_plan.md:290`); the paper-facing-gate section, addendum V2, goal_template, README, and adversarial prompt all say the same thing. The infeasibility audit is complete with hashed inventories (`prelaunch_audits.md:5-22`), and I independently confirmed 156 source problems (`data/bench/VerilogEval-Spec-to-RTL`, 156 `_prompt.txt`) and 156 archived evaluated summaries at the named conference root. No live pending-holdout gate remains; residual holdout mentions are in the append-only history, correctly unrewritten.
4. **Deterministic tie rules.** Unique insertion index is the pinned total order in addendum (lines 26, 31, 36-37), plan (step 3/4), and goal_template; UUID is audit-only everywhere. `ArchiveMember` already carries `insertion_index` (`qd/archive.py:471`), so the rule is directly implementable. Note `ranked_front` preserves input order, so the addendum's "position in the list being ranked" and the plan's "index in the ranked list" coincide.
5. **Accurate classic-vs-Pareto mechanism disclosure.** Verified against `algorithm.py`: scalar-score-weighted roulette parent selection (`:3888-3907`), C-F distinct-parent resampling (`:3909-3921`), C-F disabled below two successes (`:3872-3873`), survivor pool = `success_pool + new_offspring` with seeded shuffle then stable score-descending sort (`:4048-4052`), explicit best-by-score plus per-metric champion lane (`:4064-4106`), `-inf` failure scores (`:5009-5045`), previous fails not retained. Every element of the addendum's disclosure (lines 53-60) matches the code. The affine-invariance argument for negative-raw objectives is also mathematically sound: normalized gain `(ref−gen)/|ref|` is a positive affine transform of `−gen`, and `_crowding_distances` is per-axis range-normalized (`qd/archive.py:456`), so Pareto selection is invariant to the synthetic reference that genuinely does distort classic's scalar (`algorithm.py:816-826`).
6. **Fresh matched classic gating.** Consistent across config (`gate: fresh_matched_classic`), addendum, plan comparator contract, and the recorded rationale (historical vLLM revision unprovable, `prelaunch_audits.md:91-93`). Historical seed-1001 values are diagnostic-only and the 90% arithmetic is exact (0.1114014700215706 × 0.9 = 0.10026132301941354). Config `max_valid_ppa_deficit: 3` and addendum "at least four tasks lower → stop" are the same rule.
7. **`algorithm.py` byte-for-byte.** Recomputed SHA-256 matches the recorded `78ebc901…` exactly; `src/revolution/pareto_revolution/` does not yet exist; HEAD is dfc130338c with a clean tracked tree.

**Additionally verified:** all seven locked-artifact hashes recompute to their recorded values (narrative rev 3 ACCEPTED, seed manifest, legacy holdout, 46-task manifest, V2 config, addendum V2, engine); the 46-task manifest is exactly the 50 minus the frozen four; the v1 config exists so the `supersedes` pointer and CLAUDE.md's version-bump rule are honored; GUIDELINES.md and AGENTS.md are identical to CLAUDE.md (no conflicting practices); the narrative rules the plan cites (missing-treatment-as-loss, reason codes, `REF_WIN`/`NEW_OK` branch table) exist at `journal_narrative.md:170-263`; `strict_ablation`, the discriminated `--search_mode` choices list, and `eoh_success_operator_set` all exist in `scripts/run_backend.py`, making `revolution_pareto` a natural one-value extension; and the separately named coverage metrics with focused tests landed in the reporting surfaces (`scripts/backend_comparison_report.py:34,62,644`, `tests/scripts/test_backend_comparison_report.py`).

## BLOCKING findings

None.

## OPTIONAL findings

1. **Smoke evidence-role wording is inconsistent across documents.** `goal_template.md:51` says "screens are technical only"; `plan.md:286` says "no performance inference"; but `claims_addendum_v2.md:68` and `v2.yaml:29` say "technical **and directional**" — "directional" licenses a weak performance inference the other two documents forbid. Minimal fix: harmonize all four to "technical evidence only; no performance inference."
2. **Smoke budget is not frozen.** Plan Stage 2 says "bounded budget" but neither the config's `small_check` block nor the addendum pins the smoke's population/generations. The still-open TODO item "Freeze the one Pareto arm, commands, output roots…" should explicitly pin the smoke budget before the run so no post-hoc flexibility exists even on technical evidence.
3. **Duplicated-loop provenance for the isolated engine.** Classic parent and survivor selection are inline in one large generation method (`algorithm.py:3869-3921, 4048-4129`), so the byte-for-byte constraint forces the Pareto engine to copy loop code rather than override two methods — an accepted, documented tradeoff (`reviews/20260713_claude_prelaunch_review.md:39-47`). Minimal hardening: record a line-provenance diff (copied ranges + source commit) in the implementation history when the engine lands, so the pre-seed-1001 audit can verify only selection logic differs; matched evaluator counts and operator audits catch budget drift but don't localize prompt/UCB-accounting drift inside a copied loop.
4. **Tournament sampling distribution and RNG stream are implied, not pinned.** "Standard binary tournament … sample two distinct contestants without replacement" (`plan.md:134-136`) doesn't state uniform draws or that they come from the run-seeded RNG. One sentence ("contestants are drawn uniformly from the run-seeded RNG") closes the last selection-rule degree of freedom.
5. **Positive-vs-parity label at exact gate equality.** `goal_template.md:18` requires a "parity" result to be labeled supporting, while the promotion gates pass at exact equality (`require_*_at_least_*`), so an all-metric exact tie satisfies both labels. Practically negligible for float HV, but one clarifying sentence (e.g., "positive requires strictly greater mean HV; coverage metrics may tie") removes the ambiguity, since coverage counts are integers where ties are likely.
6. **`restart_handoff.md:21` Next Action 1 ("Commit the V2 contract correction") is already satisfied by dfc130338c** — refresh this one line on the next touch so the handoff reflects that the pending step is this re-review and then implementation.
