# T99 Visual Inspection Notes

## Phase 03.1 Viewer

Inspected:

```text
visualizations/qd_ppa_viewer/screenshot.png
```

The full viewer renders with compare mode, archive panes, raw/improvement/
normalized coordinate controls, native timeline selector, PPA/Pareto pane, and
validation controls visible. The default `Prob015_multi_pipe_8bit` view has
zero HV for both methods, so it is a completeness screenshot rather than the
clearest result figure.

## Direct PPA Supplement

Inspected:

```text
visualizations/direct_ppa_pareto/screenshot.png
```

The static `Prob045_alu` area-power front is clear and presentation-readable.
It shows the main negative result visually: classic has a broader valid-PPA
front and more candidate coverage, while T99 has a narrower front with only
`9` valid-PPA rows.
