# T75 Tables

| File | Purpose |
| --- | --- |
| `descriptor_probe_source_aligned_shape_density_3d.json` | Confirms the descriptor requires source-aligned RTL only and does not consume PPA, synthesis, simulation, graph metrics, or final labels. |
| `t75_method_contract.json` | Compact method settings for reviewers and scripts. |

Live-run result tables belong in a later matched comparison subdirectory.
