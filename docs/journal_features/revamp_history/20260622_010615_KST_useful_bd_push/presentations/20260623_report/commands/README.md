# Commands

This directory stores exact command templates and completed command logs for
the milestone.

| File | Purpose |
| --- | --- |
| `screening_v0.md` | Preflight and small T26-family screening commands. |
| `full_rtllm_v0.md` | Full 50-problem RTLLM classic-vs-selected-QD protocol. |

Copy completed command output summaries and run roots back into this directory
after each run. Raw run artifacts stay under `exp/useful_bd_push/`.
