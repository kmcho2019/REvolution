# Visual Inspection Notes

## `deepgate_embedding_pca.png`

- The title and axis labels are readable.
- The two embedded problems use distinct colors and the legend is clear.
- The points do not collapse into one dot, which supports the nonconstant
  generated-embedding conclusion.
- The figure intentionally exposes the limitation: only two problems are
  present, so this is a bridge diagnostic, not a final comparison plot.
