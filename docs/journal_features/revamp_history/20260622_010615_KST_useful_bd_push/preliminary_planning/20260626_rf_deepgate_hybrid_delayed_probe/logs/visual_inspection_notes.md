# T96 Visual Inspection Notes

## Full Phase 03.1 Viewer

Path:
`visualizations/qd_ppa_viewer/screenshot.png`

The compare-mode screenshot is nonblank and readable. It shows both archive
panels, the PPA/Pareto pane, timeline controls, coordinate tabs, color modes,
rank filters, and method labels. The QD archive side shows occupied RF/DeepGate
hybrid cells, while classic remains visible as the PPA comparator.

## Direct PPA Supplement

Path:
`visualizations/direct_ppa_pareto/screenshot.png`

The representative `Prob045_alu` gain-space area-power plot is clear. It shows
the main failure mode: both methods reach near-max power gain, but classic has
many more valid candidates and covers a taller area-gain range than T96.

## Read

The visual evidence is consistent with the aggregate metrics. T96 has a few
small problem-level wins, but it does not recover broad front material on the
larger RTLLM cases.
