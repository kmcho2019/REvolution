# Sequential DeepSeq BD Artifacts Manifest

Status: scaffold only.

Record run roots, state graph extraction command, stimulus seeds, DeepSeq code
revision or surrogate training command, feature/cache hashes, generated tables,
generated figures, and git commit hashes.
