# T76 Figures

No figures are generated for T76. This package is a model-artifact verification
gate, so the primary evidence is the model inventory and prediction tables.

A future live tree-leaf BD package should add candidate-distribution plots
showing leaf-cluster occupancy, PPA fronts, and Phase 03.1 viewer artifacts.
