# DE-HNN Hypergraph BD Artifacts Manifest

Status: scaffold only.

Record run roots, netlist extraction command, hypergraph schema, surrogate
training command if used, model/cache hashes, generated tables, generated
figures, dependency versions, and git commit hashes.
