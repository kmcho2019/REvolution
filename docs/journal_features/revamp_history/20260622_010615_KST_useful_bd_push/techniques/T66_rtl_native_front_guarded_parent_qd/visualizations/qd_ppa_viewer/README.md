# T66 Phase 03.1 QD/PPA Viewer

Files:

- `index.html`: full linked archive/PPA viewer.
- `manifest.json`: dataset and control manifest.
- `datasets/*.json`: one dataset per hard/tuning problem.
- `validation.json` and `validation.md`: strict validator output.
- `screenshot.png`: inspected compare-mode screenshot.
- `descriptor_cache.json`: descriptor projection cache.

Strict validation passed with `scripts/validate_qd_ppa_visualization.py`.
The screenshot shows compare mode, archive projection panes, the PPA/Pareto
pane, raw/improvement/normalized coordinate controls, and raw A-P front mode.
