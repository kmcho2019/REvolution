# T44 Figures

No final figures exist yet. The smoke run generated no valid PPA candidate, so
there is no meaningful raw area-power PPA-front figure to inspect.

The full live result must add a standalone raw area-power PPA Pareto-front PNG
before any BD/archive visualization is interpreted. It must also add the
paired `visualizations/direct_ppa_pareto/` supplement and the full
`visualizations/qd_ppa_viewer/` Phase 03.1 bundle before the package can be
marked complete.
