# Simple Yosys Stat BD Artifacts Manifest

Status: scaffold only.

Record:

- run roots under `exp/useful_bd_push/simple_yosys_stat_bd/<timestamp>/`;
- exact Yosys script and Yosys version;
- descriptor extraction command;
- PCA/CVT fit paths and hashes;
- raw candidate manifest paths;
- table and figure paths;
- git commit hashes for code and docs used by each run.
