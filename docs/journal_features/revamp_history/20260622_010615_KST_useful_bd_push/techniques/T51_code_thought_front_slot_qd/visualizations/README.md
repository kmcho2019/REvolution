# T51 Visualization Plan

Status: seed `1001` visualization artifacts generated.

This directory contains:

- `direct_ppa_pareto/` with `index.html`, `metrics.json`, and
  `screenshot.png`;
- `qd_ppa_viewer/` with the full Phase 03.1 export, 13 datasets, validation
  files, and `screenshot.png`.

The direct PPA supplement is mandatory for paper-readable area-power front
inspection. The full viewer is mandatory if T51 emits usable archive artifacts.
