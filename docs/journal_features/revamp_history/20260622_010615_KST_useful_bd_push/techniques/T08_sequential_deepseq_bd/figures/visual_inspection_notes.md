# T08 Visual Inspection Notes

Inspected on 2026-06-26.

| Figure | Inspection |
| --- | --- |
| `t08_t63_state_pipeline_metric_delta.png` | Clear aggregate bar chart. It makes the T63 pattern easy to read: best-score gain, valid-PPA parity, and HV/front losses. |
| `t08_t67_seeded_thought_metric_delta.png` | Clear aggregate bar chart. The yield-positive/front-negative tradeoff is immediately visible. |
| `t08_t73_shape_density_occupancy.png` | Wide grouped bar chart is readable at full width. It clearly shows shape-density axes reducing T72 descriptor collapse. |
| `t08_t75_hv_delta_by_problem.png` | Per-problem HV deltas are readable. It clearly shows the few local wins and the large `Prob041_traffic_light` loss. |

These figures are acceptable for method-package evidence. For a presentation,
T63/T67 should be redrawn into one shared summary panel with consistent labels.
