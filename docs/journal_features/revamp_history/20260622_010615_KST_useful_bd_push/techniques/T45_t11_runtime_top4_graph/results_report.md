# T45 Results Report

Status: pre-registered; no live result yet.

T45 is the compact follow-up to T44. It keeps the same archive substrate,
model, subset, seed, budget, operator, and visualization gates, but replaces
`t11_runtime_top8_graph` with `t11_runtime_top4_graph`.

The result is not interpretable until these exist:

- `tables/t45_pareto_front_validation.json`;
- `tables/t45_candidate_ppa_points.csv`;
- `figures/t45_raw_area_power_fronts.png`;
- `visualizations/direct_ppa_pareto/index.html`;
- `visualizations/direct_ppa_pareto/screenshot.png`;
- `visualizations/qd_ppa_viewer/index.html`;
- `visualizations/qd_ppa_viewer/validation.json`;
- `visualizations/qd_ppa_viewer/screenshot.png`.

## Phase 03.1 Viewer Status

The full viewer is mandatory for a completed T45 package because the QD arm
has archive artifacts. The direct raw-PPA HTML supplement is required too, but
it is not a substitute for the linked archive/PPA viewer.

## Tier Decision

Pending.
