# MasterRTL SOG BD Artifacts Manifest

Status: scaffold only.

Record run roots, parser/lowering command, unsupported construct list, feature
schema hashes, generated tables, generated figures, dependency versions, and
git commit hashes.
