# T60 RTLTimer Timing-Risk BD Artifacts Manifest

Status: scaffold only.

Record:

- RTLTimer or RTLTimer-style source checkout path and commit hash;
- isolated uv environment path and dependency lock output;
- preprocessing command and unsupported-construct log;
- feature schema hash for `tables/rtl_timer_features.csv`;
- run roots under `exp/useful_bd_push/`;
- `/v1/models` preflight capture for any live run;
- validation commands and outputs;
- figure-generation commands;
- direct PPA and Phase 03.1 viewer paths.

No artifact is allowed to use defaulted reference PPA for a headline aggregate.
Designs with missing reference PPA must be labeled `diagnostic_only`.
