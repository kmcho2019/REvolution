# T52 Tables

Status: pre-registered.

This directory freezes the T52 hard/tuning subset and method matrix before the
live result is observed. Packaged result tables will live under
`hard_tuning_package/tables/`.
