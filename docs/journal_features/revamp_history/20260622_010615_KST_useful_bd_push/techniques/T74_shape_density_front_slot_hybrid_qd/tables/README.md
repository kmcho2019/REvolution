# T74 Tables

| File | Purpose |
| --- | --- |
| `descriptor_probe_source_aligned_shape_density_3d.json` | Probe output proving the descriptor is source-aligned RTL-only and does not require PPA/synthesis/simulation. |
| `t74_method_contract.json` | Frozen method contract for the registered live screen. |

Live-run result tables belong in a future `matched_classic_comparison/tables/`
subdirectory after T74 runs.

Expected post-run audit outputs:

- `t74_two_parent_descriptor_audit.csv`;
- `t74_two_parent_descriptor_audit_summary.json`.
