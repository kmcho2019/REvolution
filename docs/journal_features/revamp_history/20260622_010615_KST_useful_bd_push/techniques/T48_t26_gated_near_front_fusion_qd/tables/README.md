# T48 Tables

Required after the run:

- per-problem/seed metrics;
- aggregate metric deltas versus classic and T47 exact T26;
- validity gate table;
- parent-gate counter table;
- candidate-level raw PPA table for figure regeneration.

Keep every table under this technique directory and put live run outputs under
`exp/useful_bd_push/`, not under `/aux`.
