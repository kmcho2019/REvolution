# T77 Visual Inspection Notes

Inspected: `figures/t77_area_leaf_variation_gate.png`.

The figure is readable at normal zoom. The left panel shows visible variation
across generated RTL candidates and features. The right panel clearly shows
the variation-gate collapse: `17` unique feature rows but only `1` prediction
and `1` leaf row. Axis labels are dense but legible enough for a verification
package. A paper figure should simplify candidate labels if this result is
included in slides.
