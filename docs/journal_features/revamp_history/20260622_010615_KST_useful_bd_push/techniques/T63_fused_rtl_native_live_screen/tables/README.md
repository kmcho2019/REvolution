# T63 Tables

Present:

- `hard_tuning_subset.yaml`: frozen 13-problem hard/tuning comparator surface.
- `descriptor_probe_fused_rtl_state_pipeline_2d.json`: descriptor-axis probe
  proving the primary profile is live-runnable and PPA-free.

Pending after live run:

- final analysis tables;
- validity and PPA completeness tables;
- front, HV, HV-AUC, archive, and duplicate/family tables.
