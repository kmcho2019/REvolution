# T75 Visual Inspection Notes

- `t75_hv_delta_by_problem.png` is readable and clearly shows the main result:
  T75 has positive deltas on a few problems, but the traffic-light loss is the
  dominant negative bar.
- `t75_valid_ppa_counts.png` is readable and shows the yield story: T75 has
  more valid-PPA samples in aggregate, but the per-problem pattern is mixed.
- `pareto_examples/rtllm_prob045_alu_gain_power_area.png` is nonblank and
  useful for showing that several methods occupy similar gain clusters while
  T75 has a visible ALU HV improvement.

No figure is sufficient by itself for promotion; the package decision comes
from the matched reference-complete tables.
