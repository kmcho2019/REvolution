# DeepGate Family BD Artifacts Manifest

Status: scaffold only.

Record run roots, Yosys/ABC conversion command, AIG graph paths, DeepGate code
revision or checkpoint, dependency install commands, embedding cache hashes,
generated tables, generated figures, and git commit hashes.
