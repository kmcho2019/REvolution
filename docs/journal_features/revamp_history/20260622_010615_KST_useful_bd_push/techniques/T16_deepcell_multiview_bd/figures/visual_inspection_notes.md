# T16 Visual Inspection Notes

The copied figures were manually inspected.

- `t16_t14_hypergraph_hv.png` is readable and shows the T14 hybrid replay
  ranking.
- `t16_t14_hypergraph_pareto.png` is dense but useful: it shows representative
  raw PPA fronts and supports the direct-front-hit caveat.
- `t16_t95_deepgate_direct_ppa.png` is readable and shows the DeepGate live
  archive arm below classic on the representative ALU case.
- `t16_t99_aurora_direct_ppa.png` is readable and shows the raw
  implementation-view arm below classic on the representative ALU case.
