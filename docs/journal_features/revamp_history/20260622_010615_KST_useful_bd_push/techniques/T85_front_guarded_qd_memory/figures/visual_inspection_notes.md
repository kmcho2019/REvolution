# T85 Figure Inspection Notes

Inspected figures:

- `warmup4_prob045_gain_power_vs_area.png`
- `warmup4_prob015_gain_power_vs_effective_clock_period.png`

The figures are readable for preliminary evidence. They use shared axes,
reference stars, candidate counts, and point colors tied to PPA score.

The titles are too long for presentation slides because they use the full
backend slug. Use a shorter display label such as `FG-QDM` when promoting the
figure into a slide or report.
