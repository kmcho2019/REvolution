# T39 Results Report

Status: pre-registered; live run not started.

T39 is the sparse-yield follow-up to T38. It keeps the
`elite_pareto_slot` champion-plus-one-front-slot archive rule and lowers only
the grid-quantile warmup threshold from `8` to `4`.

The primary question is whether `Prob015_multi_pipe_8bit` can retain active
archive members under the same population, generation count, seed, descriptor,
and model configuration that left it with zero active archive members in T38.

Tier decision: pending.
