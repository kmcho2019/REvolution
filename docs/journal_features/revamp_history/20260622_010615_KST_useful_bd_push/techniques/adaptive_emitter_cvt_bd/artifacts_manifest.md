# Adaptive Emitter CVT BD Artifacts Manifest

Status: scaffold only.

Record run roots, descriptor source, centroid hashes, emitter mixture, parent
selection logs, ablation commands, generated tables, generated figures, and git
commit hashes.
