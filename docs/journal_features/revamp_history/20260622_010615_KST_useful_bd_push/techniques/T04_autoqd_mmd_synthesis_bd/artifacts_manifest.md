# AutoQD MMD Synthesis BD Artifacts Manifest

Status: scaffold only.

Record run roots, feature schema hashes, random seeds, random-feature matrix
hashes, PCA/CVT fit hashes, generated tables, generated figures, dependency
versions, and git commit hashes.
