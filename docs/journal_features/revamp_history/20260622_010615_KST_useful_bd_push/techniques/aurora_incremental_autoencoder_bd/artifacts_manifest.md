# AURORA Incremental Autoencoder BD Artifacts Manifest

Status: scaffold only.

Record run roots, feature schema hashes, train/validation/holdout splits,
training commands, refresh checkpoints, model hashes, generated tables,
generated figures, dependency versions, and git commit hashes.
