# T29 Visualizations

Status: pending live execution.

If T29 produces archive descriptors and candidate PPA rows compatible with the
Phase 03.1 schema, package a local `qd_ppa_viewer/index.html` bundle here after
the run. Otherwise, document why the viewer could not be generated and keep
the direct PPA-front PNGs in `../figures/`.
