# T100 Visual Inspection Notes

Status: inspected.

## Direct PPA Supplement

Path:
`visualizations/direct_ppa_pareto/screenshot.png`

The traffic-light area-power panel is readable. It shows the intended
mechanism-level message: RF-leaf FG-QDM improves over the SR and random
FG-QDM smoke variants on this panel, but classic still has broader visible
area/power spread and stronger front coverage.

The axes, colorbar, method labels, and candidate counts are legible. The plot
is suitable as a supporting figure for a diagnostic FG-QDM discussion, not as
a positive promotion figure.

## Phase 03.1 Viewer

Path:
`visualizations/qd_ppa_viewer/screenshot.png`

The viewer renders in compare mode with archive panes, controls, PPA/Pareto
distribution, axis labels, and summary cards visible. The RF-leaf archive
cells are visible for the QD arm; classic archive projection is intentionally
not claimed because RF leaf-ID descriptors cannot be honestly recovered for
classic candidates through the generic graph-metric path.

Use the viewer for mechanism inspection only. Do not use it to claim classic
archive-cell parity.
