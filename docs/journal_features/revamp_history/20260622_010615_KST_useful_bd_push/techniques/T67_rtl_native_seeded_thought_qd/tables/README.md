# T67 Tables

Current files:

| File | Purpose |
| --- | --- |
| `hard_tuning_subset.yaml` | Frozen 13-problem hard/tuning surface shared with T47-T66. |
| `descriptor_probe_fused_rtl_state_pipeline_2d.json` | Descriptor probe proving the RTL-native axes and no PPA requirement. |

Run result tables are pending until seed `1001` completes. The completed
package must add `t67_ppa_completeness.csv`, aggregate metrics, per-problem
metrics, thought/sample counters, and candidate-level PPA data sufficient to
regenerate the direct raw PPA figures.
