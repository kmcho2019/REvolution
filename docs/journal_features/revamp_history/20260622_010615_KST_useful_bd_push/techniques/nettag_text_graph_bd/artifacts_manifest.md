# NetTAG Text-Graph BD Artifacts Manifest

Status: scaffold only.

Record run roots, graph extraction command, text attribute schema, tokenizer or
hashing settings, model code revision if used, embedding cache hashes,
generated tables, generated figures, and git commit hashes.
