# Goal Template

Use this text for `/goal`. It omits the literal `/goal` prefix. Keep the
detailed plan, method list, and policies in sibling docs as the full contract.

```text
Objective: on branch feat/journal-useful-bd-exp-20260622, make a wide-net,
persistent push to find useful behavior descriptors for QD/MAP-Elites in RTL
netlist evolution and PPA optimization. Use
docs/journal_features/revamp_history/20260622_010615_KST_useful_bd_push/ as the
revamp root. The plan is the contract, TODO is the checklist, history is the
audit log, and the metrics, anti-gaming, code, visualization, vLLM, and
strategy docs are binding guardrails.

Outcome: find at least one descriptor/archive-coupling technique that is
near-classic or better, or produce a rigorous negative map of failed technique
families. The 10% threshold is only a strong-win tier. Keep investigating
methods that match classic within tolerance, preserve classic-covered
valid-PPA designs, or improve HV, valid-PPA yield, Pareto spread, archive
coverage, lineage yield, or AUC.

Current interpretation: classic REvolution is a strong small-budget hill
climber. QD should not replace that pressure; it should act as auxiliary
memory that preserves PPA-competitive RTL implementation families under
validity constraints. Do not overclaim generic MAP-Elites success from archive
occupancy, yield-only gains, or defaulted-reference aggregates.

Current priority: finish the active useful-BD packages and
presentations/20260623_report/. T75 is packaged as a positive diagnostic, not
a promotion. T76 partially verifies MasterRTL tree artifacts, and T77 retires
direct pretrained Area-head leaves after generated-candidate collapse. Next,
pre-register a fixed-total-budget shape ablation or reproduce timing/power
feature flows before any live pretrained-BD spend.

Metrics: never use average fitness or average best PPA as primary evidence.
Use reference-complete paired subsets for headline classic-vs-QD claims.
Report global PPA hypervolume, HV-AUC, passive archive QD score/coverage,
Pareto/front-family spread, valid-PPA yield, duplicates, and retained classic
coverage.

Required scope: attempt at least 10 numbered technique packages before any
broad negative sign-off, including simple controls, synthesis/netlist
descriptors, learned/projection descriptors, RTL-native descriptors, and
archive-coupling/Pareto variants. Every package needs methodology,
artifacts, commands, figures, tables, report, visual notes, and a tier.

Constraints: keep model, subset, seeds, prompts, operators, budget, timeouts,
and evaluation flow fixed unless a versioned exception is recorded before
running. Preflight vLLM and record /v1/models for live runs; use 128000 token
budgets except tiny smokes. Freeze subsets before reading outcomes. Do not use
final PPA, reference PPA, fitness, hypervolume, Pareto rank, or test pass rate
as in-loop BD inputs. Invalid candidates and duplicates never count as useful
diversity. T1+ must preserve every classic-covered design. 50%+ validity/yield
drops are visible warnings when classic has at least 10 passing samples.

Iteration policy: move from prior evidence to controls, synthesis/netlist
descriptors, source-aligned RTL-native descriptors, projection/codebook
methods, MOME/adaptive CVT, and encoders. MasterRTL/RTLTimer claims must
separate extractor/count features from verified pretrained use. Before a
pretrained descriptor, record commits/hashes, load weights, assert schema,
reproduce upstream inference, then try prediction-risk, tree-leaf, or margin
embeddings without PPA leakage. Use isolated uv envs or source checkouts when
dependencies block progress. Keep code skimmable, update docs, avoid broad
fallbacks/back-compat clutter, commit regularly, and inspect messages.

Blocked stop condition: stop only after three concrete attempts hit the same
blocker, with commands, artifacts, missing input, and exact next decision.
Do not stop because one method is weak. Completion requires focused checks,
inspected figures, precise conclusions, no overclaimed tier, no anti-loophole
violation, and PASS from the adversarial prompt.
```
