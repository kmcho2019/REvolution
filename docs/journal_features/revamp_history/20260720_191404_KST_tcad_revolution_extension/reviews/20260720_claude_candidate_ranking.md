# External Candidate-Ranking Review

- Date: 2026-07-20
- Reviewer: Claude Code, Sonnet, high effort
- Mode: read-only plan mode
- Timeout: 600 seconds
- Scope: candidate ranking, contracts, conference/component audits, H5-H7
  cards, registry, portfolio, and implementation paths
- Initial verdict: `FAIL`

The review ran no benchmark and edited no repository file. It independently
checked the cited classic code against the frozen engine hash and checked six
related-work citations. It found no fabricated citation or code-behavior
contradiction.

## Blocking Findings

1. `conference_method_audit.md` retained stale pre-regeneration no-C-F AUC and
   confidence-interval values that disagreed with five canonical documents.
2. H6 had a terminal `VIABLE` label without prospective current-program gates
   or the required independent review record. Its known historical outcome
   cannot be retroactively qualified under the new state machine.

## Major Findings

1. The rubric allowed H7 to pass despite a zero hardware/CAD-grounding score.
2. H7 consumed a full candidate-wave position despite being only a low-novelty
   component control.
3. H5 relied on descriptive rates without checking whether classic fail-pool
   UCB already converged toward M-F in the archived logs.
4. The provisional package risked combining three individually modest ideas;
   H7 did not fit H5's operator-role narrative.

## Minor Findings

1. H1-H3 retirement prose had not reached their registry or decisions.
2. H8 appeared in ranking but not the canonical registry.
3. The `Heuristic` line citation extended beyond the class body.

## Dispositions

| Finding | Disposition |
| --- | --- |
| Stale no-C-F numbers | `ACCEPT`: replace with the canonical fixed-denominator result and log the correction. |
| Retroactive H6 outcome | `ACCEPT`: classify as `HISTORICAL_SUPPORT` outside the candidate state machine. |
| H7 rubric/budget role | `ACCEPT`: require nonzero hardware grounding and move H7 outside both waves as a deferred diagnostic. |
| Missing UCB diagnostic | `ACCEPT`: report classic per-generation failed-parent allocation before H5 becomes `READY`. |
| Weak combined package | `ACCEPT`: Wave 1 contains H5 only; historical evidence cannot rescue it. |
| H1-H3/H8 state drift | `ACCEPT`: close proposal decisions and add H8 to the registry. |
| Line citation | `ACCEPT`: narrow the range to the class body. |

## Required Closure

H5 may become `READY` only after the corrections above, the classic UCB
trajectory artifact, and named TCAD novelty plus hardware/EDA methodology
reviews are recorded. This review does not claim that H5 is sufficient
standalone TCAD novelty; it approves only the corrected process for deciding
that question.
