# External Candidate-Ranking Closure

- Date: 2026-07-20
- Reviewer: Claude Code, Sonnet, high effort
- Mode: read-only plan mode
- Timeout: 600 seconds
- Live spend: none
- Package closure: `PASS`
- H5 TCAD novelty/naturalness verdict: `ACCEPT`

The reviewer re-read the corrected ranking, contracts, audits, registry,
portfolio, H5-H7 records, and prior external review. It verified that every
prior blocking, major, and minor finding is resolved:

1. canonical no-C-F AUC and interval values now agree;
2. no-C-F is `HISTORICAL_SUPPORT`, not retroactive `VIABLE` evidence;
3. the rubric requires nonzero hardware/CAD grounding;
4. H7 is outside both candidate waves;
5. classic UCB trajectory evidence shows no sustained M-F concentration;
6. the proposed primary package depends on H5 alone;
7. H1-H3 and H8 have consistent terminal registry records; and
8. the `Heuristic` code citation is narrowed to the class body.

## H5 Verdict

H5 clears the pre-implementation novelty/naturalness gate as a modest, natural
core correction:

- Its static binary role constraint is concretely different from COEVO's
  continuous correctness, category rewards, annealed gate, and adaptive route.
- No H5 treatment outcome existed when the current gates were frozen.
- The mechanism follows from prompt semantics plus permitted classic-only
  evidence, not from a benchmark-specific outcome.
- One subclass and one search-mode discriminant isolate the treatment without a
  new prompt, reward, public knob, archive, or copied generation method.

## Residual Risks

Novelty remains thin at 1/2 and cannot be inflated into a general adaptive
repair contribution. Observational evidence is resolved for RTL-simulation
repair association but not valid-PPA yield. These limitations do not block a
controlled experiment; they constrain the eventual claim. Hardware/EDA
methodology review and post-diff code-isolation review remain independent
requirements.
