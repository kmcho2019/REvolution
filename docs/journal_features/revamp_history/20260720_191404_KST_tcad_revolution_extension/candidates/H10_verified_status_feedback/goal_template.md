Execute H10 under the shared candidate goal contract and the frozen Wave-2
methodology. Preserve classic REvolution, its default config, and all default
prompts byte-for-byte. Implement only the fixed verified terminal-status line
for failed candidates, keep successful feedback and critic artifacts unchanged,
and add no scope knob, raw-log replacement, routing change, fallback, or
threshold.

Before model-backed spend, close every `READY` item in `hypothesis_card.md`,
freeze the exact six-arm budget worksheet and machine manifest, implement the
smallest isolated typed mode with exhaustive status tests and telemetry, run
focused and repository validation, and obtain independent simplicity,
scientific, evidence, and external reviews.

If admitted, run only the frozen sequential smoke and two-seed full-suite
ladder. Apply missing-as-failure, per-seed noninferiority, paired resource, and
program-ledger rules exactly. End with one terminal outcome in `decision.md`
and update the program history, negative map, claim ledger, registry, and
portfolio before another mechanism starts.
