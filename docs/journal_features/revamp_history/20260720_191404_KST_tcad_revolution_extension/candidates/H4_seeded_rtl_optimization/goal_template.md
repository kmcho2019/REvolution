# H4_seeded_rtl_optimization Goal Template

```text
/goal Reach a defensible PROMOTED, RETIRED, or BLOCKED decision for
H4_seeded_rtl_optimization according to hypothesis_card.md, the program plan, and the shared
candidate goal contract.

Implement the smallest mechanism-complete change on top of the frozen classic
REvolution baseline. Preserve equal model, LLM-call, synthesis-call, evaluator,
and benchmark conditions. Do not add problem-specific behavior, use hidden or
final-suite outcomes for tuning, substitute metrics, or omit negative results.

Add focused tests, typed narrow-state code, required-data asserts, methodology
docs, a frozen experiment manifest, and complete raw/report artifacts. Run the
smallest smoke, then the preregistered development screen. Permit at most two
written revisions. Freeze before holdout and run holdout once.

Completion is a decision.md with PROMOTED, RETIRED, or BLOCKED, supported by
quantitative results, mechanism evidence, natural-extension and novelty review,
code/docs checks, and adversarial PASS. A negative result is valid completion;
do not weaken gates to obtain promotion.
```
