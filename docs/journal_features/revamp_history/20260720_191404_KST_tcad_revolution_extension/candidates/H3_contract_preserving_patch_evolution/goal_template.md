# H3 Contract-Preserving Patch Evolution Goal

```text
/goal Decide H3 contract-preserving patch evolution under hypothesis_card.md,
../../tcad_revolution_extension_plan.md, the accepted claims contract, and
../../shared/candidate_goal_contract.md.

Do not implement the seed idea unless it first reaches READY. If selected,
execute the shared candidate loop without adding fallback, problem-specific
state, or unregistered revisions. Completion requires reproducible artifacts,
review dispositions, portfolio updates, and exactly one terminal outcome:
PAPER_CANDIDATE, VIABLE, RETIRED, or BLOCKED.
```
