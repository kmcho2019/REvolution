# Program Goal Template

Do not launch this while `intake_review.md` has unresolved activation blockers.
Prefer separate candidate goals after the baseline and claims contracts are
frozen.

```text
/goal Complete the REvolution TCAD extension program specified in
docs/journal_features/revamp_history/20260720_191404_KST_tcad_revolution_extension/tcad_revolution_extension_plan.md.
The end state is a minimal,
reproducible journal-extension system assembled only from independently
PROMOTED components, with a frozen full-suite evaluation, methodology,
conference-to-journal contribution delta, and claim/evidence ledger.

Do not force a positive result. Every candidate must end in PROMOTED, RETIRED,
or BLOCKED based on its preregistered gates. Work only from frozen development,
holdout, and final manifests. Preserve equal primary comparison budgets, do not
add problem-specific logic, do not use final-suite results for tuning, and do
not hide missing or negative outcomes.

Keep the program TODO concise and record all decisions, commands, failures,
config/commit hashes, and evidence in the histories. Between iterations, remove
the highest-risk scientific or reproducibility gap, not the easiest checklist
item. Include a component in the integrated system only when isolated and
interaction evidence supports it.

Completion requires passing code quality and documentation checks, reproducible
paper tables, supported core claims, and PASS from the adversarial validator.
If no algorithmic candidate passes holdout after the allowed attempts, stop as
PIVOT_REQUIRED with a complete retire report rather than claiming a journal-
ready positive extension.
```
