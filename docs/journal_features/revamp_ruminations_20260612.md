› I think we want to setup new goal for ourself but before doing so we want to sketch our idea into something more detailed . The problem is that some of the core ideas and implementation for our REvolution journal extension has been implemented as can be seen in /workspace/docs/journal_features . Some of the changes were done to cover and improve upon some of the limitations mentioned by reviewrs for the
  original submission. some of these criques and limitations were that 1) the current implementaiton relies too much of weighted sum of ppa improvements so it does not capture the pareto front or the distrubtion that well the weighted sum in our case the average is a hyperparmeter that can bias search 2) Some of the evolutionary operator choices seem rather arbitary and just inherited from earlier work like EoH
  (evolution of heuristics), 3) also it is mentioned in paper that it formulates the evolutionary operator via multi-armed bandit and use RL-based reward mechanism and softmax-ucb to determine which evolutionary operator to use at one time but it doesn't seem to be supported with ablation or experiments 4) though this paper focuses on PPA a lot the benchmarks we are dealing with are relatively small RTLLM and
  VerilogEval, 5) traditional evolutionary tries to emphaisze diversity of features like using island based or even qd (quality diversity) which are not implemented here. To address these suggestions we have been implementing some features in the journal extension such as the 01_bd_trio where we introduc qd map-elites algorithm with the features being logic_depth, ff_depth, and comb_width_log so that our
  evolutionary process can be done with more diversity, we also implemented quantile based binning, and adaptive rebinning features reagarindg map-elites implementation. Another point was that to get around the weight fitness score metric we implemented pareto_front_archive where each bin/cell of map-elites have their own pareto front representation where each bin/cell can hold up to 5 candidates which are
  selected based on nsga-ii metrics so that pareto dominating solutions within that feature representation cell is kept so there is no need for dedicated fitness score or hyperparameters and we can evolve across the entire pareto front adressing the criticism. Also regarding the evolutionary operator cricism we unified evolutionary operator into a single prompt where the llm has more freedom to decide what needs
  to happen autotnomously so we don't need to worry about prompt/evolutionary operator selection and we just have a single unified prompt that asks llm to address the feedback and improve the metrics, we also implemented thought only unification to get away from legacy thought + code EoH driven system where the evolutionary unit is thought. The idea was that we are practically speaking what we should be evolving
  is architectural direction and implementations represented by high level idea/spec of thought and code is just a physical manifestation of that so evolving the thought seems more relevant idea. So we would generate code based on our thought itself and if some of them fail functionality or synthesis PPA extraction then we would give some chance to repair it so that thought could have valid code representation.
  Regarding the benchmark limitation we took tepid steps to also integrate CVDP tepidly but they are at the draft stage and they are not really validated to work that well with our current flow and PPA extraction, so we would need to modify our evolutionary structure to also support cvdp style input output interaction structure  and also wire how we can extract ppa from it also for cvdp no reference ppa exists so
  we might need to implement an alternative system for cvvdp where it is possible to rate the ppa of sample without it (previously we used metric of how much ppa improved compared to reference rtl) the current file itself is located in /workspace/data/bench/cvdp/cvdp_v1.0.2_nonagentic_code_generation_no_commercial.jsonl the official code in ./exp/cvdp_benchmark there is a lot of version of cvdp available but we
  decided to start with nonagentic code generation no commercial as our framework is not that agentic and we don't use commericial tools (yosys, iverilog, and openroad). We also implemented tepidly older branch that attempts to integrate RealBench benchamrk which is also a newer benchmark with larger sizes and potentially more meaningful PPA metrics that can leverage the strength of our repo the branch itself
  older one is located in /workspace/.worktrees/realbench_integration_draft and the benchmark files are located in ./exp/RealBench. Our goal in the new improvement revamp push is to fully integrate these benchmarks and dmeonstrate meaningful result such that our models are able to pass and have meaningful metrics while also demosntrating to reviewrs that we have integrated and tested against harder and more
  difficult RTL based benchmarks with larger designs that make it more meaingful to compare PPA improvement against. For models we should use these models. As for the LLM models we have available primarily stick with the local model that is running in our vllm instance available thorugh curl http://host.docker.internal:8000/v1/models . The only caveat being that we should use long max_model_len at least 128k as
  this is a reasoning model and may require longer context length to operate correctly.
  But for more difficult tasks that are more difficult to run we may try other models like deepseek-v4-pro available via deepseek api provider in our llm.py the API key is loaded as DEEPSEEK_API_KEY env variable in the .env so that for harder tasks we can try this. Also for this model also set max_tokens / max_model_len to be very long at least 128k or 256k some cases as thins may require long thinking for the
  tasks to compelte. Use more capable models when the local model isn't able to fix and solve a intial problem and fall into doom loop. I think for our plan we should set out very detailed specifications of what we want to be accompolished and what are the exact quantative gates that must be accomplished before we consider it completed and done. For each major milestone and things we want to accomplish the plan
  must spejcificy in detail the quanatative condition like figures that to meet, it should be difficult to hack and get around so that the intent of our plan is actually accompolished. For some of the benchmark integrations for example we want to say that we should be able to do full end-to-end evolutionary run with the benchmarks with at least some candidates passing for our run and demosntat meaningful
  improvement in tersm of pass rate, number of problems with at least one candidate passing, and demonstrate ppa improvement over generations with might also implemtn summary generation script dedicated to the benchmarks as we have for verilogeval and rtllm. Though we have came up with ideas for journal extension and actually implemented them in code form there are several blockers and issues that must be
  overcome some of the blockers are that the performance itself tends to lag compared to our classic REvolution baseline we have conducted tests expeically hard-subset tests where we test against more difficult rtllm and verilogeval benchamrks problems and try to compare pass rate and ppa improvement but the version that implements our complete list of features like
  `grid_quantile_pareto_journal_bd_unified_rebin_on` feature set as recorded in ./docs/journal_features tend to underperform compared to classic baselines. I think our goal is to have has actually improve some of the kinks and bugs and other problems with current implementation so that the performance such as pass rate, synthesis rate, the PPA improvement rates, pareto merics like hypervolume, etc.  are actually
  compettive and improved compared to classic revolution baseline so that our journal extension is actually meaingful and reviers could signoff on it. Another problem is that for map elites quality diversity we implemnted we said that we chose logic_depth, ff_depth, and comb_width_log but when we chose it and apoplied it we found that in our visualization tool documnted in /workspace/docs/
  journal_features/03_1_pareto_visualization_resources there is often time collapse and it was not that clear it was the best choice in terms of representing diversity of RTL designs another cricisim was that it wasn't really clear what basis theoreical emprical analytical these feature set was based upon (we probably should develop a good rationale justifiacation that can sell current feature set to reviewrs or
  we can develop a new feature set that performs well in terms of ppa and diversity and could be justified to reviewrs in a powerful and perusasive manner as part of the overall journal narrative) and a journal would need a strong storyline to stand on its own feet and pass reviewr muster but it seemed a bit weak according to some people. And the final issue is that I think the 'storyline' and core narrative
  thesis is a bit lacking. That is one of the more important issues that should be resolved with our push where we need to demosntrate to our reviewers why our work is necessary, why it is important, and why our contributions and methodolgoy is relevant to this and what kind of analyitcal thoeretical persusasive basis are there behind it I think that is relatively lacking and I think haveing a coherent powerful
  one that can bind our journal extension + features together is cruicial and we should aslso develop a core journal narrative story telling file in form of md that is well formatted detail but not too verbose that can bind the changes we made together. WE have some core points we have made to our implementation but if some changes are needed to accomdate the coherent and powerful journal narrative some changes
  could be made though it should be accomdoated. Also we should have stict gating for the core narrative piece spec sheet before it can be accepted I think we can use deidcated adversarial sub-agents just for reviewring and signing off on the sotryline narrative portion. I think we should have multiple sub-agents with different prompts with different background preconceptions approaches thinking sort of which
  might be relevant for TCAD and have these subagents being stand ins signing off, too avoid excessive sub-agent submissiveness and non-criticalness we should also have more critical al la Reviewr 2 style reviewrs who are more skeptical going in and require higher standards, to ensure that the core narrative does not fall into group think and is genuinuly creative powerful and persuasive and natural and not
  awkward, while being relatively simple and clear.